---
git_repo: ssh://git@stash.sharplabs.com:7999/sasbd/ssg.git

project_name: bigdata
application_name: bigdata

# Application bigdata
django_settings_file: settings

run_django_syncdb: yes
run_django_south_migration: no
run_django_collectstatic: yes
# run_bower_install: yes

# SMTP settings.
smtp_host: smtp.gmail.com
smtp_user: example.user.name@gmail.com

port_ssh: 22
port_http: 80
port_http_debug: 8000
port_https: 443
port_postgres: 5432


# dbs_to_backup: []
dbs_to_backup:
  - djangodb


# not used
#private_path: private

# Placeholders below for secrets that should be encrypted or stored outside repo
# Override these in private/vars.yml if you want git-crypt to encrypt them ans store them in repo

db_password: plain_text_password
db_name: database_name_within_postgres_server
ssl_key_password: plain_text_password
smtp_password: plain_text_password
django_secret_key: '1234567891230913098987qwertyuipasdfasdfasd^-*$=()!'
# Username with sudo privileges on the remote machine. Will be used by ansible to sudo stuff.
# If not specified `root` is the default
sudo_user: root
# the name of the user on the remote system that you are using to ssh into it as, e.g. `ssh remote_user@remote_server.com`
remote_user: root
# postgres_sudo_user: postgres

